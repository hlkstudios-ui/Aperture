import { adminCatalogFetch } from "@/app/lib/admin-catalog";
import { requireAdminSession } from "@/app/lib/admin-session";
import { StudioShell } from "@/app/studio/components/studio-shell";
type Item={id:string;email:string;plan_name:string;plan_code:string;status:string;provider:string;current_period_end:string|null;cancel_at_period_end:boolean;updated_at:string};
export default async function SubscriptionsPage(){const [admin,result]=await Promise.all([requireAdminSession(),adminCatalogFetch<{items:Item[];total:number}>("/admin/support/subscriptions")]);return <StudioShell admin={admin} active="subscriptions" eyebrow="Provider-authoritative billing" title="Subscriptions"><p className="editor-intro">Operational subscription visibility. Status changes arrive from the billing provider and are not manually overridden here.</p><section className="editor-panel"><p className="eyebrow">{result.total} subscriptions</p><div className="table-scroll"><table><thead><tr><th>Customer</th><th>Plan</th><th>Status</th><th>Provider</th><th>Period end</th></tr></thead><tbody>{result.items.map(item=><tr key={item.id}><td>{item.email}</td><td>{item.plan_name}</td><td>{item.status}{item.cancel_at_period_end?" · cancels at period end":""}</td><td>{item.provider}</td><td>{item.current_period_end?new Date(item.current_period_end).toLocaleDateString():"—"}</td></tr>)}</tbody></table></div></section></StudioShell>}
