import Link from "next/link";
import { adminCatalogFetch } from "@/app/lib/admin-catalog";
import { requireAdminSession } from "@/app/lib/admin-session";
import { StudioShell } from "@/app/studio/components/studio-shell";
type Snapshot={bucket:string;available:boolean;versioning:string;registered_bytes:number;asset_states:Record<string,number>;processing_jobs:number;recent_failures:Array<{id:string;filename:string;reason:string|null;updated_at:string}>};
function bytes(v:number){if(v<1024)return `${v} B`;if(v<1024**2)return `${(v/1024).toFixed(1)} KiB`;if(v<1024**3)return `${(v/1024**2).toFixed(1)} MiB`;return `${(v/1024**3).toFixed(2)} GiB`}
export default async function StoragePage(){const [admin,s]=await Promise.all([requireAdminSession(),adminCatalogFetch<Snapshot>("/admin/support/storage")]);return <StudioShell admin={admin} active="storage" eyebrow="Media durability" title="Storage"><p className="editor-intro">Registered media inventory and object-store safety signals. Credentials and object keys are never exposed.</p><section className="analytics-kpis"><article><small>Object store</small><strong>{s.available?"Available":"Unavailable"}</strong></article><article><small>Versioning</small><strong>{s.versioning}</strong></article><article><small>Registered bytes</small><strong>{bytes(s.registered_bytes)}</strong></article><article><small>Processing jobs</small><strong>{s.processing_jobs}</strong></article></section><div className="editor-columns"><section className="editor-panel"><h2>Asset states</h2><dl>{Object.entries(s.asset_states).map(([state,count])=><div key={state}><dt>{state}</dt><dd>{count}</dd></div>)}</dl><p>Bucket: {s.bucket}</p></section><section className="editor-panel"><h2>Recent failed assets</h2>{s.recent_failures.length?<ul>{s.recent_failures.map(a=><li key={a.id}><strong>{a.filename}</strong><br/>{a.reason??"No failure reason recorded"}</li>)}</ul>:<p className="studio-empty-inline">No failed assets.</p>}<p><Link href="/studio/uploads">Open uploads</Link> · <Link href="/studio/processing">Open processing</Link></p></section></div></StudioShell>}
